---
description: "Diagnose a stuck POD store: find the failing gate, one job. Never scale before ROAS holds."
argument-hint: "CPC, CR, AOV, ROAS, catalog size"
---

Run skill `diagnose-store` on `$ARGUMENTS`. Read `${CLAUDE_PLUGIN_ROOT}/knowledge/concepts/conversion-diagnosis.md`. No shell. Tag claimed.
